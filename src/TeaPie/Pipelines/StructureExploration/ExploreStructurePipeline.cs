using TeaPie.Pipelines.Base;

namespace TeaPie.Pipelines.StructureExploration;
internal class ExploreStructurePipeline : BasePipeline
{
    public static ExploreStructurePipeline CreateDefault()
    {
        var pipeline = new ExploreStructurePipeline();
        // pipeline.AddStep();
        // pipeline.AddStep();
        // pipeline.AddStep();
        // pipeline.AddStep();

        return pipeline;
    }
}
